# === DATABASE CONFIG ===
spring.datasource.url=jdbc:postgresql://localhost:5433/cocktailzclean
spring.datasource.username=postgres
spring.datasource.password=Elkapitan16
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / HIBERNATE ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === LOGGING SQL ===
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.jdbc.datasource=debug

# === JWT CONFIG ===
spring.security.jwt.secret=zeergeheimekeyzeergeheimekey123456789
spring.security.jwt.expiration=86400000

# === FILE UPLOAD ===
file.upload-dir=uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# === SERVER PORT ===
server.port=8080

thecocktaildb.api.url=https://www.thecocktaildb.com/api/json/v1/1

